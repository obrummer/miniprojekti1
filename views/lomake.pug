extends layout

block content
    br
    h1= title
    br
    p Welcome #{nimi} to #{title}
    br
    form#lomakeid(method="post", action="/addperson")
        input#lomakenimi(type="text", placeholder="Nimi", name="name")
        br
        button#lomakesubmit(type="submit") Luo
    include includes/foot.pug
